apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: usuarios-dev
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/xElGrego/gitops-lab
    targetRevision: HEAD
    path: apps/usuarios/overlays/dev

  destination:
    server: https://kubernetes.default.svc
    namespace: usuarios-dev

  syncPolicy:
    syncOptions:
      - CreateNamespace=true
